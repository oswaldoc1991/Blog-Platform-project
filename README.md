involves user authentication, basic operations such as(Create, Read, Update, Delete), text editing, and deployment
to access the basic functionality of the blog platform the programmer would have to go to the blog-platform folder
then install all of the package.json, package-lock.json and reactg for it to work, this will ultimately followed by
running the npm run dev or the npm run preview command on the terminal to see all of the current work. 

thank you for taking a look at it and hope you like it.
